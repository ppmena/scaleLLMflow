audit_model_registry    Audit every registered scale and model prompt.
available_models        List trained or fallback model prompt folders
                        for a scale.
available_scales        List scales available in the local prompt
                        registry.
build_scale_prompt      Append article text to a scale prompt.
extract_article_text    Extract text from a supported article file.
extract_pdf_text        Extract text from a PDF article.
parse_scale_scores      Parse scale item scores from an LLM response.
resolve_prompt          Resolve the best prompt registered for a scale
                        and requested model.
run_article             Run one article file through a registered scale
                        prompt.
run_dataset             Run article files in a directory through a
                        registered scale prompt.
run_llm                 Call a supported LLM provider with user-owned
                        API credentials.
scaleLLMflow-package    General LLM Workflows for Scientific Quality
                        Scales
strip_references_section
                        Remove reference sections from extracted
                        article text.
