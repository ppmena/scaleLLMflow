% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{run_dataset}
\alias{run_dataset}
\title{Run article files in a directory through a registered scale prompt.}
\usage{
run_dataset(
  articles_dir,
  scale = "mqs",
  provider = "gemini",
  model = "gemini-3.6-flash",
  output_dir,
  registry_dir = NULL,
  filetype = "auto",
  strip_references = TRUE,
  max_articles = 0,
  items = NULL,
  temperature = 0,
  top_p = 0.1,
  timeout = 300,
  api_key = NULL,
  project_id = NULL,
  max_retries = 3,
  retry_wait_seconds = 1,
  retry_backoff = 2,
  rate_limit_seconds = 0,
  reference_csv = NULL,
  validation_mode = c("free", "reference")
)
}
\arguments{
\item{articles_dir}{Directory containing articles.}

\item{output_dir}{Directory where audit logs and CSV are written.}

\item{filetype}{One of \code{"auto"}, \code{"pdf"}, \code{"txt"}, or \code{"md"}.}

\item{max_articles}{Maximum pending PDFs to process. \code{0} means all.}
}
\value{
A list containing the timestamped run directory, consensus report,
consolidated evidence report, and errors when any occurred.
}
\description{
Existing \verb{*_AuditLog.txt} files larger than 100 bytes are reused and skipped.
}
